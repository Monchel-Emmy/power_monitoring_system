# Server
PORT=4000

# MongoDB (Atlas or local)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/powermonitoring?retryWrites=true&w=majority

# Auth (use a long random string in production)
JWT_SECRET=your-super-secret-key-change-in-production

# Optional: restrict CORS to your frontend (e.g. https://your-app.vercel.app)
# FRONTEND_ORIGIN=https://your-app.vercel.app

# Email Configuration for Real Email Service
# Uncomment and fill these values to send real emails instead of using ethereal.email

#Gmail Configuration (Recommended for development)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=tuyisengeemmanuel1999@gmail.com
EMAIL_PASS=mlgw jtky mihy kvwy

# SendGrid Configuration (Alternative)
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=apikey
# EMAIL_PASS=YOUR_SENDGRID_API_KEY

# Outlook/Hotmail Configuration
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@outlook.com
# EMAIL_PASS=your-password
